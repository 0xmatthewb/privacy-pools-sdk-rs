{
  "name": "@0xbow/privacy-pools-sdk",
  "version": "0.1.0-alpha.1",
  "private": false,
  "description": "React Native package for the Privacy Pools SDK",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/0xmatthewb/privacy-pools-sdk-rs.git",
    "directory": "packages/react-native"
  },
  "main": "./src/index.ts",
  "react-native": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "prepare:package": "cargo run -p xtask -- react-native-package",
    "prepare:package-release": "cargo run -p xtask -- react-native-package --release --with-native",
    "prepare:package-release:ios": "cargo run -p xtask -- react-native-package --release --with-ios-native",
    "prepare:package-release:android": "cargo run -p xtask -- react-native-package --release --with-android-native"
  },
  "files": [
    "src",
    "android",
    "ios",
    "PrivacyPoolsSdk.podspec",
    "react-native.config.js",
    "README.md"
  ],
  "peerDependencies": {
    "react": ">=18",
    "react-native": ">=0.79"
  }
}
