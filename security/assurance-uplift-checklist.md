# Assurance Uplift Checklist

This checklist tracks the original "World-Class Assurance Uplift Plan for
Privacy Pools Rust SDK" against the current branch head. A row is only `done`
when the code/docs exist in the repo and the associated tests or workflows pass
on the pushed head.

| item | status | evidence | notes |
| --- | --- | --- | --- |
| keep PR focused on deterministic merge safety | done | `.github/workflows/ci.yml`, `xtask/src/main.rs` | Rust PR is split into focused subgroup jobs and browser/React Native PR lanes stay narrow. |
| move cargo-vet to deeper lanes | done | `xtask/src/main.rs`, `.github/workflows/assurance-nightly.yml`, `.github/workflows/release.yml` | PR no longer runs `cargo-vet`; nightly and release still do. |
| move cargo-deny advisories to deeper lanes | done | `xtask/src/main.rs` | PR runs `cargo-deny-policy`; advisories remain nightly/release only. |
| move mutation testing to deeper lanes | done | `xtask/src/main.rs`, `.github/actions/install-rust-ci-tools/action.yml`, `docs/assurance-review-guide.md` | `cargo-mutants-high-risk` remains nightly/release only and is provisioned only in the deeper CI tool profiles. |
| keep heavy browser worker/direct-execution coverage out of PR | done | `xtask/src/main.rs`, `packages/sdk/test/browser-direct-execution.test.mjs`, `packages/sdk/test/browser-worker.playwright.mjs` | PR browser lane stays deterministic; heavy runtime coverage is nightly/release only. |
| keep full mobile evidence ingestion out of PR | done | `xtask/src/main.rs`, `.github/workflows/mobile-smoke.yml` | PR React Native lane stays package/runtime smoke; full four-surface mobile evidence is a deeper lane. |
| keep long-running cross-runtime parity and compatibility checks out of PR | partial | `xtask/src/main.rs`, `packages/sdk/scripts/compare-rust-stateful.mjs`, `docs/v1-js-compatibility-matrix.md` | Stateful wrapper differentials and v1 compatibility checks stay nightly/release only, but the new same-head nightly/release proof is still pending. |
| add dedicated fuzz lane | done | `.github/workflows/assurance-fuzz.yml`, `xtask/src/main.rs`, `fuzz/Cargo.toml` | Dedicated scheduled/manual fuzz workflow is live and nightly skips embedded fuzz. |
| keep release evidence architecture as a strength | done | `.github/workflows/release.yml`, `xtask/src/main.rs`, `docs/release-process.md` | Provenance, SBOM, signed-manifest, browser package binding, and mobile evidence remain release gates. |
| add Rust-native differential/reference tests for Merkle witness derivation | done | `crates/privacy-pools-sdk-tree/src/lib.rs`, `crates/privacy-pools-sdk/tests/reference_vectors.rs` | Tree property test and spec-backed Rust reference tests cover Merkle witness derivation. |
| add Rust-native differential/reference tests for manifest/artifact verification | done | `crates/privacy-pools-sdk/tests/reference_vectors.rs`, `fixtures/spec/protocol-vectors.json` | Spec-backed Rust tests verify signed-manifest/artifact bundle behavior. |
| add Rust-native differential/reference tests for proof/session/handle lifecycle | done | `crates/privacy-pools-sdk/tests/session_lifecycle_reference.rs`, `fixtures/spec/session-lifecycle.json` | Reference tests cover session preparation, prove/verify, tamper rejection, and cache invalidation. |
| add Rust-native differential/reference tests for execution policy decisions | done | `crates/privacy-pools-sdk-chain/tests/execution_policy_reference.rs`, `fixtures/spec/execution-policy.json` | Reference tests cover strict happy path and wrong chain/root/code-hash/signer failures. |
| add stateful Rust-vs-wrapper differential cases | partial | `packages/sdk/scripts/compare-rust-stateful.mjs`, `packages/sdk/scripts/stateful-trace.mjs`, `fixtures/spec/stateful-wrapper-parity.json`, `crates/privacy-pools-sdk-cli/src/main.rs` | Dedicated Node and React Native stateful lifecycle traces now pass locally; same-head nightly/release workflow proof is still required before this row can be `done`. |
| add property tests with checked-in regression seeds for serialization/wire invariants | done | `crates/privacy-pools-sdk-core/tests/wire_property.rs`, `crates/privacy-pools-sdk-core/tests/execution_policy_wire_property.rs`, `crates/privacy-pools-sdk-core/proptest-regressions/` | Core wire/execution policy invariants now have property coverage and checked-in regressions. |
| add property tests with checked-in regression seeds for signed-manifest tamper invariants | done | `crates/privacy-pools-sdk-artifacts/src/lib.rs`, `crates/privacy-pools-sdk-artifacts/proptest-regressions/lib.txt`, `crates/privacy-pools-sdk/tests/reference_vectors.rs` | Artifacts crate property tests plus spec-backed error semantics cover signed-manifest tamper paths. |
| add property tests with checked-in regression seeds for session/handle invalidation rules | done | `crates/privacy-pools-sdk/tests/session_property.rs`, `crates/privacy-pools-sdk/proptest-regressions/session_property.txt` | Session cache invalidation and bounded reuse are property-tested with regressions checked in. |
| add property tests with checked-in regression seeds for execution-policy rejection rules | done | `crates/privacy-pools-sdk-chain/tests/execution_policy_property.rs`, `crates/privacy-pools-sdk-chain/proptest-regressions/execution_policy_property.txt` | Chain preflight mismatch handling is property-tested with checked-in regressions. |
| add property tests with checked-in regression seeds for artifact/bundle binding invariants | done | `crates/privacy-pools-sdk-artifacts/src/lib.rs`, `crates/privacy-pools-sdk-artifacts/proptest-regressions/lib.txt` | Bundle hash/signature invariants are already property-tested and now explicitly tracked. |
| add interop/spec suites as first-class checked-in fixtures | done | `fixtures/spec/protocol-vectors.json`, `fixtures/spec/session-lifecycle.json`, `fixtures/spec/execution-policy.json` | New spec fixtures are consumed directly by Rust tests. |
| include public signals and proof canonicalization vectors in spec fixtures | done | `fixtures/spec/protocol-vectors.json`, `crates/privacy-pools-sdk/tests/reference_vectors.rs` | Proof formatting/public signal vectors are first-class spec inputs. |
| include signed-manifest verification vectors in spec fixtures | done | `fixtures/spec/protocol-vectors.json`, `crates/privacy-pools-sdk/tests/reference_vectors.rs` | Signed-manifest payload/signature/public-key fixtures are first-class spec inputs. |
| include circuit artifact hashes / verification-key expectations in spec fixtures | done | `fixtures/spec/protocol-vectors.json`, `crates/privacy-pools-sdk/tests/reference_vectors.rs` | Bundle expectations include version, kinds, filenames, and sha256 values. |
| include normalized wrapper-visible error semantics in spec fixtures | done | `fixtures/spec/protocol-vectors.json`, `crates/privacy-pools-sdk/tests/reference_vectors.rs` | Canonical error substrings are tracked in spec fixtures and asserted in Rust tests. |
| smoke packaged artifacts directly in release lanes | done | `.github/workflows/release.yml`, `xtask/src/main.rs`, `docs/release-process.md` | Release assurance validates packaged artifacts, npm tarballs, and auditable CLI outputs. |
| add reviewer-facing assurance/security guide | done | `docs/assurance-review-guide.md`, `README.md`, `SECURITY.md` | Guide is published and linked from the main docs/security surfaces. |
| maintain an internal audit ledger | done | `security/audit-ledger.md`, `README.md`, `RELEASE_CHECKLIST.md` | Internal ledger is checked in and incorporated into review/release guidance. |
| keep cargo-deny / dependency-check / zizmor and treat workflow trust as first-class | done | `xtask/src/main.rs`, `.github/workflows/ci.yml`, `docs/dependency-audit.md` | Policy and workflow trust checks remain in the assurance catalog. |
| add auditable release-binary metadata | done | `.github/workflows/release.yml`, `.github/actions/install-rust-ci-tools/action.yml`, `docs/release-process.md` | Release SBOM bundle now includes an auditable CLI binary. |
| keep `xtask assurance` separated by correctness / policy / heavy runtime / fuzz / release evidence | done | `xtask/src/main.rs`, `.github/workflows/ci.yml`, `docs/assurance.md` | The shared assurance catalog still owns the checks, but PR CI and the deeper lanes now expose those concern boundaries explicitly instead of hiding them in one monolithic job. |
| make assurance findings self-contained with per-check JSON and logs | done | `xtask/src/main.rs`, `.github/workflows/ci.yml` | All aggregate lanes emit findings, per-check JSON, logs, and failure attribution artifacts. |
| add slowest-check reporting per lane | partial | `xtask/src/main.rs`, `cargo test -p xtask` | Assurance index/findings now include top-5 slowest checks; still needs same-head CI artifacts after push. |
| path-gate cheap non-core checks | partial | `.github/workflows/ci.yml`, `zizmor .github/workflows` | `docs-check` and `zizmor` are now selectively gated in the PR Rust policy lane; still needs a same-head PR CI run after push. |
| avoid one giant job hiding many failure classes | done | `.github/workflows/ci.yml`, `xtask/src/main.rs` | Rust PR lane is split into core/generated/policy groups with one merged required gate. |
| dedicated fuzz corpus layout checked in | done | `fuzz/corpus/**`, `fuzz/.gitignore` | Checked-in corpora are committed and replayed by the fuzz workflow. |
| dedicated fuzz targets for parsers / manifests / artifacts / session invalidation / execution policy | partial | `fuzz/fuzz_targets/*.rs`, `fuzz/corpus/**`, `fuzz/Cargo.toml`, `target/assurance-fuzz-semantic-smoke` | Semantic artifact bundle, session invalidation, and execution-policy fuzz targets now exist and pass locally with a pinned nightly toolchain; the scheduled workflow still needs a same-head green run after push. |
| PR CI stays lean and materially faster than the old Rust lane | partial | `.github/workflows/ci.yml`, `xtask/src/main.rs` | Rust PR lane was split and sped up; latest pushed head still needs fresh CI evidence after this tranche. |
| nightly runs deep parity / policy / mobile-browser heavy suites outside PR | done | `.github/workflows/assurance-nightly.yml`, `xtask/src/main.rs` | Nightly keeps deep browser/mobile/policy coverage outside PR. |
| fuzz workflow passes on pushed head | partial | `.github/workflows/assurance-fuzz.yml` | Workflow exists; needs green same-head run after this tranche. |
| nightly and release stay green with new uplift work | partial | `.github/workflows/assurance-nightly.yml`, `.github/workflows/release.yml` | Existing lanes are aligned; same-head live runs are still required. |
| external audit/process maturity | partial | `security/audit-ledger.md`, `security/external-audit-readiness.md`, `SECURITY.md` | External audit readiness and intake process are documented, but no external cryptographic/application audit has completed yet. |
